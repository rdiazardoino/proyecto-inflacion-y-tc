/*
 * File: app/view/ViewDashlet.js
 *
 * This file was generated by Sencha Architect version 4.2.2.
 * http://www.sencha.com/products/architect/
 *
 * This file requires use of the Ext JS 6.0.x Classic library, under independent license.
 * License of Sencha Architect does not include license for Ext JS 6.0.x Classic. For more
 * details see http://www.sencha.com/license or contact license@sencha.com.
 *
 * This file will be auto-generated each and everytime you save your project.
 *
 * Do NOT hand edit this file.
 */

Ext.define('ViewDashlet.view.ViewDashlet', {
    extend: 'Ext.container.Container',
    alias: 'widget.viewdashlet',

    requires: [
        'ViewDashlet.view.ViewDashletViewModel',
        'ViewDashlet.view.ViewDashletViewController',
        'ViewDashlet.view.ContextMenu',
        'Ext.menu.Menu',
        'Ext.list.Tree',
        'DashletHTML.controller.Translator',
        'DashletHTML.controller.StorageManager',
        'ChartHTML.view.ChartBuilder',
        'GridHTML.view.GridBuilder',
        'Ext.chart.axis.layout.CombineDuplicate'
    ],

    config: {
        timestampData: -1,
        CacheStore: {
            
        },
        taskPending: [
            
        ],
        DashletInstancesStore: {
            CHART: {
                column: {
                    
                },
                bar: {
                    
                },
                line: {
                    
                },
                area: {
                    
                },
                pie: {
                    
                },
                radar: {
                    
                },
                financial: {
                    
                },
                scatter: {
                    
                }
            },
            GRID: {
                advanced: null,
                list: null,
                olap: null
            }
        },
        allowBuild: true,
        modeActive: 'normal',
        menuData: {
            hide: {
                text: 'Ocultar',
                iconCls: 'ids-vaadin-icon-eye-off',
                leaf: true,
                cmd: 'hide'
            },
            navigate: {
                text: 'Navigate',
                leaf: true,
                iconCls: 'ids-vaadin-icon-link-ext ',
                cmd: 'navigate'
            },
            export: {
                text: 'Download excel file',
                leaf: true,
                iconCls: 'ids-vaadin-icon-file-excel',
                cmd: 'excel'
            },
            exportImg: {
                text: 'Export to image',
                leaf: true,
                iconCls: 'ids-vaadin-icon-picture',
                cmd: 'exportImg'
            },
            queryDescriptor: {
                xtype: 'button',
                text: 'Query descriptor',
                leaf: true,
                iconCls: 'ids-vaadin-icon-info-circled',
                cmd: 'qd'
            },
            chart: {
                text: 'Chart',
                iconCls: 'ids-vaadin-icon-chart-line',
                leaf: true,
                children: [
                    
                ],
                cmd: 'chart',
                type: 'CHART'
            },
            grid: {
                text: 'Grid',
                iconCls: 'ids-vaadin-icon-grid',
                leaf: true,
                children: [
                    
                ],
                cmd: 'grid',
                type: 'GRID'
            },
            chartItems: [
                {
                    text: 'Column',
                    children: [
                        {
                            text: 'Clustered',
                            iconCls: 'chart-icon chart-column-clustered',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'columnclustered',
                            type: {
                                category: 'column',
                                mode: 'clustered'
                            }
                        },
                        {
                            text: 'Overlaid',
                            iconCls: 'chart-icon chart-column-overlaid',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'columnoverlaid',
                            type: {
                                category: 'column',
                                mode: 'overlaid'
                            }
                        },
                        {
                            text: 'Stacked',
                            iconCls: 'chart-icon chart-column-stacked',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'columnstacked',
                            type: {
                                category: 'column',
                                mode: 'stacked'
                            }
                        },
                        {
                            text: '100%',
                            iconCls: 'chart-icon chart-column-100',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'column100',
                            type: {
                                category: 'column',
                                mode: '100%'
                            }
                        }
                    ]
                },
                {
                    text: 'Bar',
                    children: [
                        {
                            text: 'Clustered',
                            iconCls: 'chart-icon chart-bar-clustered',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'baroverlaid',
                            type: {
                                category: 'bar',
                                mode: 'clustered'
                            }
                        },
                        {
                            text: 'Overlaid',
                            iconCls: 'chart-icon chart-bar-overlaid',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'barstacked',
                            type: {
                                category: 'bar',
                                mode: 'overlaid'
                            }
                        },
                        {
                            text: 'Stacked',
                            iconCls: 'chart-icon chart-bar-stacked',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'barstacked',
                            type: {
                                category: 'bar',
                                mode: 'stacked'
                            }
                        },
                        {
                            text: '100%',
                            iconCls: 'chart-icon chart-bar-100',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'bar100',
                            type: {
                                category: 'bar',
                                mode: '100%'
                            }
                        }
                    ]
                },
                {
                    text: 'Line',
                    leaf: true,
                    cmd: 'chart-item',
                    internalID: 'lineline',
                    type: {
                        category: 'line',
                        mode: 'line'
                    }
                },
                {
                    text: 'Area',
                    children: [
                        {
                            text: 'Overlaid',
                            iconCls: 'chart-icon chart-area-overlaid ',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'areaoverlaid',
                            type: {
                                category: 'area',
                                mode: 'overlaid'
                            }
                        },
                        {
                            text: 'Stacked',
                            iconCls: 'chart-icon chart-area-stacked',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'areastacked',
                            type: {
                                category: 'area',
                                mode: 'stacked'
                            }
                        },
                        {
                            text: '100%',
                            iconCls: 'chart-icon chart-area-100',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'area100',
                            type: {
                                category: 'area',
                                mode: '100%'
                            }
                        }
                    ]
                },
                {
                    text: 'Radial',
                    children: [
                        {
                            text: 'Pie',
                            iconCls: 'chart-icon chart-pie ',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'piepie',
                            type: {
                                category: 'pie',
                                mode: 'pie'
                            }
                        },
                        {
                            text: 'Radar',
                            iconCls: 'chart-icon chart-radar',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'radarradar_single',
                            type: {
                                category: 'radar',
                                mode: 'radar_single'
                            }
                        },
                        {
                            text: 'Bubble',
                            iconCls: 'chart-icon chart-bubble',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'scatterbubble',
                            type: {
                                category: 'scatter',
                                mode: 'bubble'
                            }
                        }
                    ]
                },
                {
                    text: 'Financial',
                    children: [
                        {
                            text: 'Candlestick',
                            iconCls: 'chart-icon chart-candlestick ',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'financialcandlestick',
                            type: {
                                category: 'financial',
                                mode: 'candlestick'
                            }
                        },
                        {
                            text: 'OHLC',
                            iconCls: 'chart-icon chart-ohlc',
                            leaf: true,
                            cmd: 'chart-item',
                            internalID: 'financialohlc',
                            type: {
                                category: 'financial',
                                mode: 'ohlc'
                            }
                        },
                        {
                            text: 'XY',
                            leaf: true,
                            iconCls: 'chart-icon chart-xy',
                            cmd: 'chart-item',
                            internalID: 'scatterxy',
                            type: {
                                category: 'scatter',
                                mode: 'xy'
                            }
                        }
                    ]
                }
            ],
            gridItems: [
                {
                    text: 'Advanced',
                    iconCls: 'grid-icon grid-advanced',
                    leaf: true,
                    cmd: 'grid-item',
                    internalID: 'advanced',
                    type: 'advanced'
                },
                {
                    text: 'List',
                    iconCls: 'grid-icon grid-list',
                    leaf: true,
                    cmd: 'grid-item',
                    internalID: 'list',
                    type: 'list'
                },
                {
                    text: 'Olap',
                    iconCls: 'grid-icon grid-olap',
                    leaf: true,
                    cmd: 'grid-item',
                    internalID: 'olap',
                    type: 'olap'
                }
            ],
            drillHistory: {
                text: 'Drill history',
                leaf: false,
                iconCls: 'ids-vaadin-icon-history',
                children: [
                    {
                        text: 'Back',
                        id: 'drill-hist-back-ref',
                        iconCls: 'ids-vaadin-icon-left',
                        leaf: true,
                        style: 'display: none;',
                        cmd: 'drill-history-back'
                    },
                    {
                        text: 'Forward',
                        id: 'drill-hist-forward-ref',
                        iconCls: 'ids-vaadin-icon-right',
                        leaf: true,
                        cmd: 'drill-history-forward'
                    }
                ]
            }
        }
    },

    controller: 'viewdashlet',
    viewModel: {
        type: 'viewdashlet'
    },
    reference: 'viewdashlet',
    border: false,
    cls: [
        'sencha-view-dashlet',
        'sencha-font-family',
        'Loader'
    ],
    style: 'position:relative;',
    defaultListenerScope: true,

    layout: {
        type: 'vbox',
        align: 'stretch'
    },
    items: [
        {
            xtype: 'contextmenu',
            flex: 1
        },
        {
            xtype: 'container',
            reference: 'altMenu',
            floating: true,
            hidden: true,
            hideMode: 'visibility',
            style: 'position: absolute; left: 0; top: 0; z-index: 5;',
            items: [
                {
                    xtype: 'treelist',
                    bind: '{selectorData}',
                    micro: true,
                    listeners: {
                        selectionchange: {
                            fn: 'selectorSelectionChange',
                            scope: 'controller'
                        }
                    },
                    reference: 'menu',
                    expanderOnly: false,
                    singleExpand: true
                }
            ]
        },
        {
            xtype: 'container',
            reference: 'corner',
            cls: 'cornerIteraction',
            itemId: 'mainMenu',
            style: ''
        },
        {
            xtype: 'container',
            flex: 1,
            reference: 'card',
            style: 'background: white;z-index:0;',
            layout: 'card',
            items: [
                {
                    xtype: 'container',
                    reference: 'error',
                    cls: 'view-dashlet-theme',
                    itemId: 'error'
                }
            ]
        },
        {
            xtype: 'container',
            reference: 'querydescriptor',
            cls: 'vd-querydescriptor',
            hidden: true,
            layout: {
                type: 'hbox',
                align: 'stretch'
            }
        }
    ],
    listeners: {
        afterrender: 'onPanelAfterRender'
    },

    onPanelAfterRender: function(component, eOpts) {

        var me = component,

            tmp,

            RestServices = component.RestServices,
            SessionData = component.SessionData,
            DashletSpec = component.DashletSpec;


        // SETEO IDIOMA DE LIFERAY
        DashletHTML.controller.Translator.setLang(me.SessionData.idsLang);


        if (me.modeActive === 'normal') {

            me.startListener(RestServices, DashletSpec);

            me.restServicesOpts = {
                Ticket: SessionData.Ticket,
                User: SessionData.User,
                query: DashletSpec.query,
                idO3ClientSSO: SessionData.idO3ClientSSO,
                url: SessionData.url,
                exportURL: SessionData.exportURL,
                withCredentials: SessionData.withCredentials,
                directCall: SessionData.directCall,
                Accept: SessionData.Accept,
                urlPrefixPortal: SessionData.urlPrefixPortal,
                idsLang: me.SessionData.idsLang,
                timeout: SessionData.viewDashlet_timeout,
                async: SessionData.async
            };

            // CONFIGURO REST SERVICES

            RestServices.setConfigs(me.restServicesOpts);

            // QUERY GUARDADA EN LOCALSTORAGE
            me.initialQueryExtension = me.initialQueryExtension || { hasInfo: false, data: [] };

            if (me.viewMode === 'VIEW') {
                RestServices.controlListener(me.initialQueryExtension.data, {
                    decode: true,
                    hasInfo: me.initialQueryExtension.hasInfo
                });
            }


            // SETEO CONFIGURACIONES FINALES

            me.heightNull = me.configs.heightNull;
            me.gridConfig = me.configs.gridOps || {};

            tmp = DashletSpec.laf.viewSpec.gridSpec;
            DashletSpec.laf.viewSpec.gridSpec = tmp === undefined || tmp === "" ? me.getGridDefaultSpec() : tmp;

            me.setSpec(DashletSpec, {
                update: true,
                excludeURN: false
            });

            // TIMESTAMP
            me.timestamp = RestServices.getTimestamp();
        }


        if (me.viewMode === "VIEW") {
            me.menuContextual = Ext.create("ViewDashlet.view.ContextMenu");

            me.menuContextual.setCfg({
                viewDashlet: me,
                portletID: me.SessionData.portletID
            });

            component.el.on({
                scope: me.menuContextual,
                contextmenu: me.menuContextual.contextMenuEvent
            });
        }


        me.menuTranslator();
        me.build();

        if (window.callPhantom){ /// PARA RENDERIZAR PHANTOMJS

            setTimeout(function() {

                window.callPhantom({
                    cmd: 'renderPage'
                });

            }, 500);
        }
    },

    build: function() {
        var me = this,
            RestServices = me.RestServices,
            DashletSpec = me.DashletSpec,
            Utils = me.Utils,
            DataJSON,
            ViewSpec = DashletSpec.laf.viewSpec,
            ChartBuilder = me.ChartBuilder,
            GridBuilder = me.GridBuilder,

            CardContainer = me.lookupReference('card'),

            jsonGridFormat = false,
            emptyResult = false,

            _temp1,
            _temp2,
            _temp3,
            cmp,
            _id,
            _index,
            _temp4;

        me.addCls("Loader");

        me.createCache(RestServices, DashletSpec);

        RestServices.getData(function(data) {

            try {

                if (me.taskPending.length >= 1) {
                    _temp1 = me.taskPending.pop();
                    _temp1();
                    return;
                }
                _temp1 = false;

                try {

                    if (typeof data.responseText === 'string') {

                        if (data.responseText.trim() === "") {

                            emptyResult = true;

                        } else {

                            DataJSON = JSON.parse(data.responseText);
                            _temp1 = (DataJSON.Fault === undefined);
                        }

                    } else if (typeof data.responseText === 'object') {

                        DataJSON = data.responseText;
                        _temp1 = (data.Fault === undefined) || (DataJSON.Fault === undefined);

                    }

                } catch (exception) {
                    _temp1 = false;
                }


                if (data.status === 200 && _temp1 && !emptyResult) {

                    me.Data = data;
                    CardContainer.setActiveItem(0);

                    //UPDATE QUERY DESCRIPTOR

                    if (me.timestampData !== RestServices.getTimestamp() && ViewSpec.showQueryDescriptor === 'true') {

                        me.lastURN = RestServices.BindingCache.queryTemplate.urn;
                        _temp1 = (
                            DataJSON.queryCommandsResult && DataJSON.queryCommandsResult.queryCommandResult[1].queryResultContext) ||
                            (DataJSON.queryCommandResult[1].queryResultContext);

                        me.buildQueryDescriptor(_temp1);

                    }

                    // CONFIGURACION DE REST SERVICES Y DASHLETS

                    switch (me.typeDashletShowed) {
                        case "CHART":

                            _temp1 = ViewSpec.chartSpec.type;
                            _temp2 = me.DashletInstancesStore.CHART[_temp1.category][_temp1.mode];

                            _id = me.modeDashletShowed.category + me.modeDashletShowed.mode.replace("%", '');

                            if (_temp2 === undefined) {

                                _temp3 = ChartBuilder.buildChart(DashletSpec, DataJSON);

                                _temp3.itemId = _id;
                                me.installListener(_temp3);

                                _temp2 = {
                                    timestamp: RestServices.getTimestamp(),
                                    instance: _temp3
                                };

                                me.timestampData = RestServices.getTimestamp();

                                CardContainer.add(_temp3);

                                CardContainer.setActiveItem(_id);


                                me.DashletInstancesStore.CHART[_temp1.category][_temp1.mode] = _temp2;

                                if (_temp3.validatorInfo.showAlternativeChart) {
                                    me.specChange = me.enableMenu("CHART");
                                }

                            } else {

                                if (_temp2.timestamp === RestServices.getTimestamp() && !me.specChange) {

                                    _temp2.timestamp = RestServices.getTimestamp();
                                    CardContainer.setActiveItem(_id);

                                } else {


                                    cmp = CardContainer.getComponent(_temp2.instance.itemId);

                                    if (cmp !== undefined) {
                                        cmp.destroy();
                                    }

                                    _temp3 = ChartBuilder.buildChart(DashletSpec, DataJSON);

                                    _temp3.itemId = _id;
                                    _temp2.instance = _temp3;
                                    me.installListener(_temp3);

                                    _temp2.timestamp = RestServices.getTimestamp();
                                    me.timestampData = RestServices.getTimestamp();

                                    CardContainer.add(_temp3);
                                    CardContainer.getLayout().setActiveItem(_id);

                                    if (_temp3.validatorInfo.showAlternativeChart) {
                                        me.specChange = me.enableMenu("CHART");
                                    }
                                }
                            }
                            break;

                        case "GRID":

                            _temp1 = me.modeDashletShowed;

                            jsonGridFormat = _temp1 === "olap";

                            _temp2 = me.DashletInstancesStore.GRID[_temp1];
                            _id = _temp1;

                            if (_temp2 === undefined || _temp2 === null) {

                                _temp4 = GridBuilder.buildGrid(DashletSpec, DataJSON, {
                                    widthViewport: me.getWidth()
                                });

                                _temp4.itemId = _id;

                                me.installListener(_temp4);

                                _temp1 = {
                                    timestamp: RestServices.getTimestamp(),
                                    instance: _temp4
                                };
                                me.timestampData = RestServices.getTimestamp();

                                CardContainer.add(_temp4);
                                CardContainer.setActiveItem(_id);

                                me.DashletInstancesStore.GRID[_id] = _temp1;

                            } else {
                                if (_temp2.timestamp === RestServices.getTimestamp() && !me.specChange) {

                                    _id = _temp2.instance.itemId;
                                    CardContainer.setActiveItem(_id);


                                } else {

                                    cmp = CardContainer.getComponent(_temp2.instance.itemId);
                                    if (cmp !== undefined) {
                                        cmp.destroy();
                                    }

                                    _temp4 = GridBuilder.buildGrid(DashletSpec, DataJSON, {
                                        widthViewport: me.getWidth()
                                    });

                                    _temp4.itemId = _id;
                                    me.installListener(_temp4);

                                    _temp1.timestamp = RestServices.getTimestamp();
                                    _temp1.instance = _temp4;
                                    me.timestampData = RestServices.getTimestamp();

                                    CardContainer.add(_temp4);
                                    CardContainer.setActiveItem(_id);
                                }
                            }
                            break;
                    }

                    /*** CONFIGURO BARRA ****/

                    if (me.specChange) {
                        me.buildMenu({
                            features: jsonGridFormat ? DataJSON.queryCommandResult[1].queryResultContext.feature : DataJSON.queryCommandsResult.queryCommandResult[1].queryResultContext.feature
                        });
                    }

                    /******* ACTUALIZO OPCIONES DE MENU *******/
                    me.updateMenu();

                    me.specChange = false;

                } else {

                    if (emptyResult) {

                        me.startErrorShow({
                            internalErrorMessage: {
                                message: DashletHTML.controller.Translator.getMessage("emptyResponse"),
                                detail: ''
                            }
                        });

                    } else {
                        me.startErrorShow(data);
                    }

                    me.removeCls('Loader');
                }
            } catch (e) {

                if (e.stack) console.log(e.stack);
                /*** GENERO COMPONENTE PARA MOSTRAR ERROR ***/

                Utils = Ext.create("DashletHTML.controller.Utils");
                _temp1 = DashletHTML.controller.Translator;

                _temp2 = document.getElementById(me.SessionData.portletID);

                if (_temp2 !== null){

                    _temp2.innerHTML = '';

                    var stack = e.stack || '';
                    stack = stack.split('\n');

                    for (_index = 0; _index < stack.length; _index++) {
                        stack[_index] = stack[_index].trim();
                    }
                    stack = stack.join("<br>");

                    cmp = Utils.generateSimpleErrorMessage(
                        _temp1.getMessage("error") + ': ' + e.message,
                        stack
                    );

                    cmp.renderTo = me.SessionData.portletID;

                    Ext.create("Ext.container.Container", cmp);


                }

            }

            if (me.renderListener){
                me.renderListener();
            }
        });
    },

    buildQueryDescriptor: function(queryResultContext) {

        var me = this,
            retval = [],
            temp,
            labelTemp,
            i,
            cubosQD,
            itemsQD,
            labelCaption,
            headerCls = 'sencha-font-size',
            valueCls = 'sencha-font-size',
            QueryDescriptor = me.lookupReference("querydescriptor"),
            arrayTemp;


        arrayTemp = queryResultContext.axis[0].dimension instanceof Array ? queryResultContext.axis[0].dimension : [queryResultContext.axis[0].dimension];

        if (arrayTemp.length > 0) {
            temp = {
                xtype: 'container',
                layout: {
                    type: 'vbox',
                    align: 'stretch'
                },
                flex: 1,
                items: [{
                    xtype: 'label',
                    cls: headerCls,
                    html: 'Columnas:'
                }]
            };

            for (i = 0; i < arrayTemp.length; i++) {
                labelTemp = (arrayTemp[i]['@label'] || arrayTemp[i].label);
                labelCaption = arrayTemp[i].currentMember.Caption;

                temp.items.push({
                    xtype: 'label',
                    cls: valueCls,
                    text: '- ' + labelTemp + ((labelTemp != labelCaption) ? (": " + labelCaption) : ""),
                    flex: 1
                });
            }

            retval.push(temp);

        }

        arrayTemp = queryResultContext.axis[1].dimension instanceof Array ? queryResultContext.axis[1].dimension : [queryResultContext.axis[1].dimension];

        if (arrayTemp.length > 0) {
            temp = {
                xtype: 'container',
                layout: {
                    type: 'vbox',
                    align: 'stretch'
                },
                flex: 1,
                items: [{
                    xtype: 'label',
                    cls: headerCls,
                    html: 'Filas:'
                }]
            };

            for (i = 0; i < arrayTemp.length; i++) {
                labelTemp = (arrayTemp[i]['@label'] || arrayTemp[i].label);
                labelCaption = arrayTemp[i].currentMember.Caption;

                temp.items.push({
                    xtype: 'label',
                    cls: valueCls,
                    text: '- ' + labelTemp + ((labelTemp != labelCaption) ? (": " + labelCaption) : "")
                });
            }

            retval.push(temp);

        }

        if (queryResultContext.measures.currentMember.Caption) {
            retval.push({
                xtype: 'container',
                layout: {
                    type: 'vbox',
                    align: 'stretch'
                },
                flex: 1,
                items: [{
                    xtype: 'label',
                    cls: headerCls,
                    html: 'Medidas:'

                }, {
                    xtype: 'label',
                    cls: valueCls,
                    text: '- ' + queryResultContext.measures.currentMember.Caption,
                    flex: 1
                }]
            });
        }

        if (queryResultContext.cubes) {

            cubosQD = {
                xtype: 'container',
                layout: {
                    type: 'vbox',
                    align: 'stretch'
                },
                flex: 1,
                items: [{
                    xtype: 'label',
                    cls: headerCls,
                    html: 'Cubos:'
                }]
            };

            itemsQD = queryResultContext.cubes.cube instanceof Array ? queryResultContext.cubes.cube : [queryResultContext.cubes.cube];

            itemsQD.forEach(function(element) {
                cubosQD.items.push({
                    xtype: 'label',
                    cls: valueCls,
                    text: '- ' + element.name + ' (' + (new Date(element.modifiedTime)).toLocaleString() + ')',
                    flex: 1
                });
            });

            retval.push(cubosQD);
        }

        QueryDescriptor.removeAll();
        QueryDescriptor.add(retval);

    },

    buildMenu: function(opts) {

        var me = this;

        if (me.viewMode === "EDIT") {

            me.lookupReference('corner').setHidden(true);

            return;
        }

        var RestServices = me.RestServices,
            SessionData = me.SessionData,
            DashletSpec = me.DashletSpec,
            IteractionSpec = DashletSpec.interactions || {},
            chartIteraction = IteractionSpec.chart || {},
            gridIteraction = IteractionSpec.grid || {},
            ViewSpec = DashletSpec.laf.viewSpec,

            menuContextual = me.menuContextual,

            menu = me.lookupReference('menu'),
            corner = me.lookupReference('corner'),

            menuData = me.menuData,

            chartType,
            gridType,
            features = {},
            contextSettings,
            featuresStore = opts.features,

            // menu store

            menuStore = {
                type: 'tree',
                root: {
                    expanded: true,
                    children: [menuData.hide]
                }
            },

            // temporales

            gridItem = null,
            chartItem = null,
            tools = null,

            toolsTemplate = {
                text: DashletHTML.controller.Translator.getMessage("menu_tools"),
                iconCls: 'ids-vaadin-icon-sliders',
                children: []
            },

            tmp,
            it,
            dashletType,
            hide,
            showSelector,

            isAdhoc = /\:ad\-hoc\:/.test(RestServices.getURN() || "");

        for (it = 0; it < featuresStore.length; it++) {
            features[featuresStore[it].name] = featuresStore[it].enabled || featuresStore[it].enabled === "true";
        }

        contextSettings = DashletSpec.contextSettings;

        // MOSTRAR / OCULTAR NAVIGATE

        it = (contextSettings.navigate === 'false' || contextSettings.navigate === undefined);

        hide = /(rule)/g.test(RestServices.getURN());


        if ( features.portletNavigation && ((!(it || hide) && !isAdhoc) || (isAdhoc && SessionData.show_adhoc_nav))  ) {

            tools = tools || toolsTemplate;
            tools.children.push(menuData.navigate);
            menuContextual.enableNavigate(true);

        }


        // MOSTRAR/OCULTAR EXPORTAR

        it = (contextSettings.exportTo === 'false' || contextSettings.exportTo === undefined);

        if (!it) {

            tools = tools || toolsTemplate;
            tools.children.push(menuData["export"]);
            menuContextual.enableExport(true);
        }


        // mostrar/ocultar query descriptor

        it = (ViewSpec.showQueryDescriptor === 'false' || ViewSpec.showQueryDescriptor === undefined);

        if (!it) {

            tools = tools || toolsTemplate;
            tools.children.push(menuData.queryDescriptor);
        }

        // para descargar imagenes
        tmp = contextSettings.exportImage || {enable: "false"};

        if (tmp.enable === 'true'){
            tools = tools || toolsTemplate;
            tools.children.push(menuData.exportImg);
        }

        showSelector = (ViewSpec.viewSelector === "true");

        dashletType = ViewSpec.type;

        if (showSelector) {

            gridItem = menuData.grid;
            chartItem = menuData.chart;
        }

        if (ViewSpec.chartSpec !== undefined && ViewSpec.chartSpec !== "") {

            chartType = ViewSpec.chartSpec.type;

            if (ViewSpec.chartSpec.layout.showTypeSelectorView === 'true') {

                if ((!showSelector && dashletType === "CHART") || showSelector) {

                    if (!showSelector) {
                        chartItem = menuData.chart;
                    }

                    chartItem.leaf = false;
                    chartItem.children = menuData.chartItems;
                }
            }
        }


        if (ViewSpec.gridSpec !== undefined && ViewSpec.gridSpec !== "") {

            gridType = ViewSpec.gridSpec.gridGeneral.gridType;


            if (ViewSpec.gridSpec.gridGeneral.typeSelector === 'true') {

                if ((!showSelector && dashletType === "GRID") || showSelector) {

                    if (!showSelector) {

                        gridItem = menuData.grid;
                    }

                    gridItem.leaf = false;
                    gridItem.children = menuData.gridItems;

                }
            }
        }


        // últimos chequeos


        if (chartItem !== null) {
            menuStore.root.children.push(chartItem);
        }

        if (gridItem !== null) {
            menuStore.root.children.push(gridItem);
        }

        if (tools !== null) {
            menuStore.root.children.push(tools);
        }

        if (chartIteraction.drill === 'true' || gridIteraction.drill === 'true') {
            menu.drillEnable = true;
            menuStore.root.children.push(menuData.drillHistory);
        } else {
            menu.drillEnable = false;
        }

        if (menuStore.root.children.length === 1) {
            if (menu !== null) {
                menu.destroy();
                corner.destroy();
            }
        } else {
            menu.setStore(menuStore);
        }


    },

    updateMenu: function() {

        var me = this,
            RestServices = me.RestServices,
            Menu = me.lookupReference('menu'),
            store,
            tmp,
            menuContextual = me.menuContextual;

        if (Menu !== null && Menu.drillEnable) {

            store = Menu.getStore();

            // BACK DRILL
            tmp = store.getNodeById('drill-hist-back-ref');
            tmp = Menu.itemMap[tmp.internalId];

            if (RestServices.hasBackForward(false)) {
                tmp.setStyle({ 'display': 'block' });
                menuContextual.hideDrillBack(false);
            } else {
                tmp.setStyle({ 'display': 'none' });
                menuContextual.hideDrillBack(true);
            }

            // FORWARD DRILL

            tmp = store.getNodeById('drill-hist-forward-ref');
            tmp = Menu.itemMap[tmp.internalId];

            if (RestServices.hasBackForward(true)) {
                tmp.setStyle({ 'display': 'block' });
                menuContextual.hideDrillForward(false);
            } else {
                tmp.setStyle({ 'display': 'none' });
                menuContextual.hideDrillForward(true);
            }

        }

    },

    initDashlet: function(loginData, spec, opts) {

        var me = this;

        me.RestServices = Ext.create("DashletHTML.store.Rest.RestServices", opts.RestServiceCfg);
        me.Utils = Ext.create("ViewDashlet.view.Utils");
        me.SessionData = loginData;
        me.DashletSpec = spec;
        me.Prefix = opts.prefixUrl;
        me.configs = opts;
        me.viewMode = opts.viewMode || "VIEW";
        me.ignoreEvent = false;
        me.timeoutActive = false;
        me.specChange = true;
        me.heightNull = opts.heightNull;

        me.ChartBuilder = Ext.create("ChartHTML.view.ChartBuilder");
        me.ChartBuilder.setChartColors(opts.defaultChartColors);
        me.ChartBuilder.setCfg({
            heightNull: me.heightNull,
            portletID: me.SessionData.portletID
        });

        me.GridBuilder = Ext.create("GridHTML.view.GridBuilder");
        me.GridBuilder.setConfigs({
            sessionId: me.SessionData.sessionId,
            viewMode: me.viewMode,
            portletID: me.SessionData.portletID
        });

        me.modeActive = 'normal';

    },

    initDummyMode: function(loginData, data, opts) {
        var me = this;

        me.RestServices = Ext.create("DashletHTML.store.Rest.RestDummy");
        me.Utils = Ext.create("ViewDashlet.view.Utils");
        me.viewMode = opts.viewMode || "VIEW";

        me.SessionData = loginData;

        me.ignoreEvent = false;
        me.timeoutActive = false;
        me.specChange = true;

        me.ChartBuilder = Ext.create("ChartHTML.view.ChartBuilder");
        me.GridBuilder = Ext.create("GridHTML.view.GridBuilder");

        me.modeActive = 'dummy';
    },

    enableMenu: function(viewType) {
        var me = this,
            DashletSpec = me.DashletSpec,
            viewSpec = DashletSpec.laf.viewSpec;

        if (viewType == "CHART" && viewSpec.chartSpec.layout.showTypeSelectorView === 'false') {
            viewSpec.chartSpec.layout.showTypeSelectorView = 'true';
            return true;
        }

        return false;
    },

    createCache: function(RestServices, DashletSpec) {
        var me = this,
            SessionData = me.SessionData,
            accept,
            forceGridMode = false,
            invertAxes = false,
            calculatedData = false,
            notCalculatedData = false,
            spec,
            queryComplement = "",
            type = DashletSpec.laf.viewSpec.type,
            dataFilters = DashletSpec.dataFilters,
            requestMode =  DashletSpec.query === undefined ? "VIEW_QUERY": "JOLAP_QUERY";

        calculatedData = dataFilters.calculatedData !== undefined && dataFilters.calculatedData === 'true';
        notCalculatedData = dataFilters.notCalculatedData !== undefined && dataFilters.notCalculatedData === 'true';

        switch (type) {
            case "CHART":
                accept = "application/is-grid+json";
                spec = DashletSpec.laf.viewSpec.chartSpec;

                if (spec.type.category === 'pie' && me.viewMode !== 'EDIT') {
                    type = 'pie';
                    invertAxes = (spec.axes.invertedAxes === 'false');
                } else {
                    type = 'chart';
                    invertAxes = (spec.axes.invertedAxes === 'true');
                }

                if (dataFilters.dataFilter[0] !== undefined && dataFilters.dataFilter[0]["@mode"] === 'CHART') {
                    calculatedData = dataFilters.dataFilter[0].calculatedData === 'true';
                    notCalculatedData = dataFilters.dataFilter[0].notCalculatedData === 'true';
                }
                if (dataFilters.dataFilter[1] !== undefined && dataFilters.dataFilter[1]["@mode"] === 'CHART') {
                    calculatedData = dataFilters.dataFilter[1].calculatedData === 'true';
                    notCalculatedData = dataFilters.dataFilter[1].notCalculatedData === 'true';
                }

                break;
            case "GRID":
                spec = DashletSpec.laf.viewSpec.gridSpec.gridGeneral;
                invertAxes = (spec.invertAxis === 'true');


                if (DashletSpec.laf.viewSpec.gridSpec.gridGeneral.gridType === "olap") {
                    accept = "application/json";
                    type = "olap";
                    queryComplement = "<queryCommand request='GRID_OLAP_PRESENTATION_TREE' />";
                } else {
                    type = 'grid';
                    accept = "application/is-grid+json";
                }

                if (dataFilters.dataFilter[0] !== undefined && dataFilters.dataFilter[0]["@mode"] === 'GRID') {
                    calculatedData = dataFilters.dataFilter[0].calculatedData === 'true';
                    notCalculatedData = dataFilters.dataFilter[0].notCalculatedData === 'true';
                }
                if (dataFilters.dataFilter[1] !== undefined && dataFilters.dataFilter[1]["@mode"] === 'GRID') {
                    calculatedData = dataFilters.dataFilter[1].calculatedData === 'true';
                    notCalculatedData = dataFilters.dataFilter[1].notCalculatedData === 'true';
                }

                break;
        }

        if (!RestServices.existeCache(type)) {
            forceGridMode = (DashletSpec.laf.viewSpec.forceGridMode === 'true');

            RestServices.createCache(type, {
                requestMode: requestMode,
                Accept: accept,
                invertAxes: invertAxes,
                forceGridMode: forceGridMode,
                calculatedData: calculatedData,
                notCalculatedData: notCalculatedData,
                urn: DashletSpec.url,
                queryComplement: queryComplement,
                query: SessionData.initialViewQuery
            });

            if (!SessionData.initialViewQuery)
                RestServices.buildRequest();

        } else {
            RestServices.bindCache(type);
        }


    },

    setData: function(newData) {
        this.Data = newData;
    },

    setSpec: function(newSpec, opts) {


        var me = this,
            RestServices = me.RestServices,
            tmp,
            ViewSpec = newSpec.laf.viewSpec,
            type = ViewSpec.type;

        me.specChange = opts.update || false;
        me.DashletSpec = newSpec;
        me.viewReference = newSpec.url;
        me.typeDashletShowed = ViewSpec.type;

        tmp = {
            urn: newSpec.url,
            forceGridMode: (ViewSpec.forceGridMode === "true"),
            calculatedData: (newSpec.dataFilters.dataFilter.calculatedData == "true"),
            notCalculatedData: (newSpec.dataFilters.dataFilter.notCalculatedData == "true")
        };

        if (opts.excludeURN) {
            tmp.urn = undefined;
        }

        if (ViewSpec.type === 'CHART') {

            if (ViewSpec.chartSpec.type.category === 'pie' && me.viewMode !== 'EDIT') {
                type = 'pie';
                tmp.invertAxes = ViewSpec.chartSpec.axes.invertedAxes === 'false';
            } else {
                type = 'chart';
                tmp.invertAxes = ViewSpec.chartSpec.axes.invertedAxes === 'true';
            }
            if (newSpec.dataFilters.dataFilter[0] !== undefined && newSpec.dataFilters.dataFilter[0]["@mode"] === 'CHART') {
                tmp.calculatedData = newSpec.dataFilters.dataFilter[0].calculatedData === 'true';
                tmp.notCalculatedData = newSpec.dataFilters.dataFilter[0].notCalculatedData === 'true';
            }
            if (newSpec.dataFilters.dataFilter[1] !== undefined && newSpec.dataFilters.dataFilter[1]["@mode"] === 'CHART') {
                tmp.calculatedData = newSpec.dataFilters.dataFilter[1].calculatedData === 'true';
                tmp.notCalculatedData = newSpec.dataFilters.dataFilter[1].notCalculatedData === 'true';
            }

        } else {
            tmp.invertAxes = ViewSpec.gridSpec.gridGeneral.invertAxis === 'true';

            if (ViewSpec.gridSpec.gridGeneral.gridType === "olap") {
                type = "olap";
            } else {
                type = 'grid';
            }
            if (newSpec.dataFilters.dataFilter[0] !== undefined && newSpec.dataFilters.dataFilter[0]["@mode"] === 'GRID') {
                tmp.calculatedData = newSpec.dataFilters.dataFilter[0].calculatedData === 'true';
                tmp.notCalculatedData = newSpec.dataFilters.dataFilter[0].notCalculatedData === 'true';
            }
            if (newSpec.dataFilters.dataFilter[1] !== undefined && newSpec.dataFilters.dataFilter[1]["@mode"] === 'GRID') {
                tmp.calculatedData = newSpec.dataFilters.dataFilter[1].calculatedData === 'true';
                tmp.notCalculatedData = newSpec.dataFilters.dataFilter[1].notCalculatedData === 'true';
            }
        }

        if (!RestServices.existeCache(type)) {
            me.createCache(RestServices, me.DashletSpec);
        }

        RestServices.bindCache(type);
        RestServices.setGlobalsParams(tmp);

        switch (ViewSpec.type) {
            case "CHART":
                me.modeDashletShowed = ViewSpec.chartSpec.type;
                break;
            case "GRID":
                me.modeDashletShowed = ViewSpec.gridSpec.gridGeneral.gridType;
                break;
        }


    },

    startListener: function(RestServices, DashletSpec) {


        var me = this,
            context = DashletSpec.contextSettings,
            SessionData = me.SessionData,
            StorageManager,
            syncName;

        if (me.viewMode === "EDIT") return;

        if (context !== undefined && context.synchronizationConfig !== undefined && context.synchronizationConfig.synchronizer !== undefined) {
            syncName = context.synchronizationConfig.synchronizer;

            StorageManager = Ext.create("DashletHTML.controller.StorageManager", {
                controllerName: syncName,
                ticket: me.SessionData.sessionId
            });

            me.StorageManager = StorageManager;
            me.initialQueryExtension = StorageManager.getSavedQuery();

            ClientCommunicationBus.addListener(me.listenEvents.bind(me), syncName);
        }

        if (context !== undefined && context.synchronizationConfig !== undefined && context.synchronizationConfig.synchronizerSend !== undefined) {
            synchronizerSend = context.synchronizationConfig.synchronizerSend;
            me.synchronizerSend = synchronizerSend;
        }


        ClientCommunicationBus.addListener(me.drillEvent.bind(me), SessionData.portletID + "-drillEvent");
    },

    updateSpec: function(type, chartSpec, gridSpec, isVisible) {
        var me = this,
            spec = chartSpec || gridSpec,

            dashletSpec = me.DashletSpec;

        if (me.DashletSpec === undefined) {
            return;
        }

        if (spec !== undefined) {
            dashletSpec.laf.viewSpec.viewSelector = spec.laf.viewSpec.viewSelector;
        }
        if (type !== undefined) {
            dashletSpec.laf.viewSpec.type = type;
        }

        if (chartSpec !== undefined) {
            dashletSpec.laf.viewSpec.chartSpec = spec.laf.viewSpec.chartSpec;
        }

        if (gridSpec !== undefined) {
            dashletSpec.laf.viewSpec.gridSpec = spec.laf.viewSpec.gridSpec;
        }

        if (spec !== undefined) {
            dashletSpec.url = spec.url;
            dashletSpec.dataFilters = spec.dataFilters;
            dashletSpec.laf.viewSpec.forceGridMode = spec.laf.viewSpec.forceGridMode;
            dashletSpec.contextSettings = spec.contextSettings;

        }

        if (!me.RestServices.waitingResponse) {

            me.isBuilding = true;
            me.setSpec(dashletSpec, {
                update: true,
                excludeURN: false
            });

            if (isVisible) {
                me.build();
            }
        } else {
            me.setTaskPending({
                DashletsManager: me,
                dashletSpec: dashletSpec,
                update: true,
                excludeURN: false,
                isVisible: isVisible
            });
        }


    },

    listenEvents: function(name, data) {

        var me = this,
            RestServices = me.RestServices;

        me.addCls('Loader');

        RestServices.controlListener(JSON.parse(data).query, {
            decode: true
        });

        if (!RestServices.isWaitingResponse()) {
            me.build();
        }

    },

    drillEvent: function(eventName, data) {
        var me = this,
            dim1 = data.dim1,
            dim2 = data.dim2,
            queryArray = [],
            query = {},
            Utils = me.Utils,
            RestServices = me.RestServices;

        me.addCls('Loader');

        if (dim1 !== undefined) {
            if (dim1.isMeasure) {

                queryArray.push(Utils.buildSingleMemberQuery(
                    Utils.escapeXML(dim1.dimensionName),
                    Utils.escapeXML(dim1.data),
                    true));

            } else {

                queryArray.push(Utils.buildDrillQuery(dim1.dimensionName, dim1.data, true));

            }
        }

        if (dim2 !== undefined) {

            if (dim2.isMeasure) {

                queryArray.push(Utils.buildSingleMemberQuery(
                    Utils.escapeXML(dim2.dimensionName),
                    Utils.escapeXML(dim2.data),
                    true));

            } else {

                queryArray.push(Utils.buildDrillQuery(dim2.dimensionName, dim2.data, true));
            }
        }

        if (me.synchronizerSend) {//tira el evento

            query.query = queryArray;
            ClientCommunicationBus.fireEvent(me.synchronizerSend, JSON.stringify(query));

        } else {

            RestServices.controlListener(queryArray, {
                decode: true,
                internalEvent: true
            });


            me.build();
        }

    },

    getGridDefaultSpec: function() {

        return {
            "gridGeneral": {
                "showTitles": "true",
                "showHorizontalAxis": "true",
                "showVerticalAxis": "true",
                "showHorizontalGridLines": "true",
                "showVerticalGridLines": "false",
                "gridType": "advanced",
                "typeSelector": "false",
                "requestFailed": "showAlert",
                "invertAxis": "false",
                "showLink": "false",
                "showLinkIn": "newTab",
                "linkIsSelector": "false",
                "showBorder": "false",
                "verticalSeparator": "false",
                "verticalSeparatorColor": "0x000000",
                "horizontalSeparator": "false",
                "horizontalSeparatorColor": "0x000000",
                "selectMultiple": "false",
                "allowSelection": "false",
                "typeOneSelected": "drill",
                "navigationLink": "self",
                "tabOpt": "_self",
                "rowSelection": {
                    "selectedRow": ""
                },
                "sorting": {
                    "showIn": "BOTH"
                },
                "display": {
                    "label": "true",
                    "value": "true",
                    "showInfoWindow": {
                        "visible": "false",
                        "link": "true",
                        "columns": {
                            "column1": {
                                "source": "data0",
                                "value": "true",
                                "title": "false"
                            }
                        }
                    },
                    "showInfoColumn": "",
                    "columns": ""
                },
                "axis": {
                    "headerColor1": "0xffffff",
                    "headerColor2": "0xe6eeee",
                    "fontType": "Myriad pro",
                    "fontSize": "11",
                    "fontColor": "0x000000",
                    "italic": "normal",
                    "bold": "normal",
                    "align": "center"
                },
                "cells": {
                    "fontType": "Myriad pro",
                    "fontSize": "11",
                    "alternatingColor1": "0xffffff",
                    "alternatingColor2": "0xeff1f2",
                    "fontColor": "0x000000",
                    "italic": "normal",
                    "bold": "normal",
                    "calculatedBackgroundColor": "0xC9B599",
                    "align": "right"
                },
                "dimensionsColumns": {
                    "fontType": "Myriad pro",
                    "fontSize": "11",
                    "fontColor": "0x000000",
                    "italic": "normal",
                    "bold": "normal",
                    "calculatedBackgroundColor": "0xC9B599",
                    "align": "left"
                },
                "columns": "",
                "visibility": {
                    "@inView": "false",
                    "@showNewCols": "true"
                },
                "sortInfo": {
                    "dummy": ""
                },
                "@name": "Grid General"
            },
            "listAttributes": {
                "columnsCount": "4",
                "autoColumn": "true",
                "rowCount": "4",
                "autoRow": "true",
                "width": "80",
                "autoWidth": "true",
                "itemWidthAutoMode": "maximum",
                "height": "80",
                "autoHeight": "false",
                "value": "data0",
                "label": "element0"
            },
            "gridTables": {
                "fontFamily": "Myriad Pro",
                "fontSize": "12",
                "headerFontFamily": "Myriad Pro",
                "headerFontSize": "14",
                "@name": "Grid tables"
            },
            "dimensions": {
                "columns": "2",
                "cube": "PedidosPendientes",
                "dimension": [{
                    "name": "Pedido",
                    "rootMember": {
                        "UName": "[Pedido][Pedido]"
                    }
                }, {
                    "name": "Cliente",
                    "rootMember": {
                        "UName": "[Cliente][Cliente]"
                    }
                }, {
                    "name": "Viaje",
                    "rootMember": {
                        "UName": "[Viaje][Viaje]"
                    }
                }]
            }};


    },

    startErrorShow: function(response) {

        var me = this,
            Utils = Ext.create("DashletHTML.controller.Utils"),
            card = me.lookupReference('card'),
            errorCnt = me.lookupReference('error');

        errorCnt.removeAll();

        errorCnt.add(Utils.generateErrorComponent(response));

        card.setActiveItem('error');
    },

    installListener: function(cmp) {

        var me = this;

        if (typeof cmp.on === "function"){
            cmp.on("activate", me.removeLoader);
        }else{
            (cmp.listeners = cmp.listeners || {}).activate = me.removeLoader;
        }

        cmp.ViewDashlet = me;
    },

    removeLoader: function(cmp) {

        var me = cmp.ViewDashlet,
            altMenu = me.lookup('altMenu'),
            corner = me.lookup('corner');

        cmp.ViewDashlet.removeCls("Loader");

        altMenu.setPosition(0, 0);

        if (corner !== null) {
            corner.el.on('click', me.cornerMouseoverEvent.bind(me));
        }

    },

    cornerMouseoverEvent: function() {

        this.lookup('altMenu').show();
    },

    menuTranslator: function() {
        var me = this,
            Translator = DashletHTML.controller.Translator,
            menuData = me.menuData;

        menuData.chart.text = Translator.getMessage("menu_chart");
        menuData.grid.text = Translator.getMessage("menu_grid");
        menuData["export"].text = Translator.getMessage("menu_excel");
        menuData.navigate.text = Translator.getMessage("menu_navigate");
        menuData.queryDescriptor.text = Translator.getMessage("menu_qd");
        menuData.hide.text = Translator.getMessage("menu_hide");
        menuData.exportImg.text = Translator.getMessage("menu_exportImg");

        menuData.drillHistory.text = Translator.getMessage("drill_history");
        menuData.drillHistory.children[0].text = Translator.getMessage("drill_back");
        menuData.drillHistory.children[1].text = Translator.getMessage("drill_forward");


    },

    setRenderListener: function(renderListener) {
        this.renderListener = renderListener;
    }

});